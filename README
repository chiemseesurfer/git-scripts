             ,
         -  /|
        -  / |
      -   /o |
   ~^~ ^~//-'|~^~ ~^~
    ~^~.-->>---. ~^ ~^~
   ~^~ `"""""""` ^~^ ~^
    ~^~ ~^ ~^~^ ~^~^ ~^
    Max Oberberger
    max@oberbergers.de

################################################

Every script has its own helpmenu (use -h or --help).

settings/git-settings.sh: (tested on osx-lion and debian)
	A script to set useful global git settings (aliases, user-name and 
	user-email,...)

repos/split-repo.sh:
	This program excludes a folder in an existing git-repository to 
	a new git-repository.

################################################
